import React from "react";

const Demo = () => {
  return (
    <div>
      这是开始的git
      <code lang="git">
        <span>git reset</span>
        <span>hard: 直接回退本地代码到指定的版本</span>
        <span>soft: 回退本地代码到指定版本，同事将后面版本的改动暂存</span>
      </code>
    </div>
  );
};

export default Demo;
